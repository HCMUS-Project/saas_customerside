/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "res.cloudinary.com", "img.freepik.com", "neomissalonandspa.com"],
  },
};

export default nextConfig;
